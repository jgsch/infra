[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "bot"
version = "0.1.0"
description = "Shared bot utilities"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["bot*"]
